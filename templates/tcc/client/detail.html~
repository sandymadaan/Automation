{% extends "base_client.html" %}

    
{% load i18n %}

{% block content %}
<h1 class="changeBlue">PERFORMA for {{user.username}}</h1>
<br>
<table> 
{% if performa %}
    {% for performas in performa %}
     <tr><td>Name :</td><td>{{performas.first_name}}</td></tr>
	 <tr><td>Address :</td><td>{{performas.address_1}},{{performas.address_2}}, {{performas.city}}({{performas.pin_code}}){{performas.state}},  {{performas.country}} </td></tr>
	 <tr><td>Contact No :</td><td>{{performas.contact_no }}</td></tr>
	 <tr><td>Website :</td><td>{{performas.website}}</td></tr>
	 <tr><td>Email_id :</td><td>{{user.email}}</td></tr>
	 <tr><td>Type of Organisation :</td><td>{{performas.type_of_organisation}}</td></tr>
       {% endfor %}
{%endif %}
     
</table>	 
{% endblock %}
