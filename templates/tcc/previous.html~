<html>
<head>
    <title>Previous Jobs</title>
</head>
<body>
<div align="center"><h2>{{ title}}</h2></div>
<div>Previous Jobs for {{ job.client__client__first_name }}
<table border='1' width='100%'>
	<tr>
		<th>S.No.</th>
		<th>Job No</th>
		<th>Material</th>
		<th>Site</th>
                
	</tr>
{% if job %}
    {% for jobs in job %}
	<tr>
			<td>{{ jobs.id }}</td>
			<td> {{ jobs.job_no }}</td>
			<td> {{jobs.site}}</td>
                        
		</tr>
        {% endfor %}
{% endif  %} 
</table>

</body>
</html>
