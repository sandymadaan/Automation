{% extends "base.html" %}
{% load i18n %}
<html>
<head>
    <!--	<title>GNDEC</title>	-->
</head>
{% block content %}
	<script type="text/javascript" src="add_row.js"></script>
<body>
	<h1>Report for Chemical Analysis</h1>

	{% if form.errors %}
        <p style="color: red;">
        	Please correct the error{{ form.errors|pluralize }} below.
        </p>
        {% endif %}

	<form action="" method="post">
	{% csrf_token %}
	<table>
		<tr>
		<td>{{ form.s_no }}</td>
		<td>{{ form.description }}</td>
		<td>{{ form.result }}</td>
		<td><INPUT type="checkbox" name="chk"/></td>
		<td>1</td>
		</tr>
	</table>
<!--	<div class="field">
        	{{ form.s_no.errors }}
        	<label for="id_subject">s_no:</label>
        	{{ form.s_no }}
        </div>
	<div class="field">
        	{{ form.description.errors }}
        	<label for="id_email">description:</label>
        	{{ form.description }}
        </div>
        <div class="field">
        	{{ form.message.result }}
        	<label for="id_message">result:</label>
        	{{ form.result }}
        </div>-->
	<INPUT type="button" value="Add Row" onclick="addRow('dataTable')" />
 
	<INPUT type="button" value="Delete Row" onclick="deleteRow('dataTable')" />	
	
        <input type="submit" value="Submit">

	</form>
</body>
</html>
{% endblock %}
